<form method="post" action="{{ path('app_admin_menu_category_delete', {'id': menu_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu_category.id) }}">
    <button class="btn">Delete</button>
</form>
